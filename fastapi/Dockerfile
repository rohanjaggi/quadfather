FROM python:3.12-slim

WORKDIR /app

COPY fastapi/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY fastapi/api/ ./api/

EXPOSE 8080

CMD uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8080}
